// Assembly-level attributes file.
// The RimWorld AssemblyResolve handler is registered in RimWorldResolverSetup.cs via a global
// NUnit [SetUpFixture] / [OneTimeSetUp], which runs before any test in the assembly executes.

