[REVIEW-impl-documentation]: CONCERNS

# Review — documentation

- **Phase**: impl-review
- **Iteration**: 01

## Findings

| # | Severity | Location | Description | Suggested fix |
|---|---|---|---|---|
| 1 | high | `design/architecture/adr/adr-0006-rimworld-assemblyresolve-setupfixture.html` (Decision, Accepted-risk, Alternatives, Acceptance) vs `Source/LordKuper.Common.Tests/RimWorldResolverSetup.cs:23-33` | **Doc drift on the implemented resolver seam.** ADR-0006 documents the decision as a namespace-less `[SetUpFixture]`/`[OneTimeSetUp]` and presents `[ModuleInitializer]` as an *anticipated fallback* "not chosen now", to be adopted "only if discovery-time resolution actually fails in impl" and "raised to the orchestrator before adopting". The shipped code adopts the fallback: `RimWorldResolverSetup.cs` carries a live `[ModuleInitializer] InitializeModule()` registering the resolver at module-load **plus** the `[OneTimeSetUp]`, with a net472 `ModuleInitializerAttribute` polyfill. So both paths ship and the module-load path is the one that actually fulfils the AC-20 "resolver-before-type-load" guarantee — yet the approved ADR still frames `[ModuleInitializer]` as unchosen. ADR is no longer actual vs implementation. (No escalation recorded: `state.json.escalations` is `[]`.) | Re-house the resolver decision in the ADR to reflect that the `[ModuleInitializer]` module-load path was adopted (alongside `[OneTimeSetUp]`, sharing the idempotency guard); move it from "anticipated fallback / not chosen" to the chosen mechanism. Routes through design / design-promote, not this review. PM must confirm the adoption was an approved decision (the plan Task 2 required orchestrator sign-off before adopting). |
| 2 | medium | `design/architecture/tech-reference/nunit-4.6.1.md:27,48` | Same drift, lower-authority doc. The NUnit reference states the RimWorld resolver "lives in a namespace-less `[SetUpFixture]`" with `[OneTimeSetUp]` and makes no mention of the `[ModuleInitializer]` module-load registration that the code actually ships. Misdescribes the implemented seam. | When ADR-0006 is corrected in design-promote, update this reference's resolver note to match (module-load `[ModuleInitializer]` + `[OneTimeSetUp]`). |
| 3 | medium | `.asd/project/commands.yaml:23` (coverage comment) | Stale coverage floor in a machine-read SSoT. The comment says "P is the line-coverage proxy tracked against the **>=80% floor**". This sprint's authoritative target (plan DoD line 41) is **>=37.2%** (sprint-001 baseline 38.2% − 1.0pp); measured this sprint is 37.05% (plan.md:107). The 80% figure is carried over from ADR-0001's original sprint-001 goal and contradicts the live target that reviewers read from this file. | Update the coverage comment to the current floor (>=37.2%) or remove the hard number and point at the plan/PRD as the floor SSoT. (commands.yaml edit is infra owned by `/asd-init` / config flow, not this review — route per artifact-layout.) |
| 4 | low | `.asd/project/custom-coding-rules.md:46` | The Testing rule states the resolver is housed "in a namespace-less (global) `[SetUpFixture]` with `[OneTimeSetUp]`" — same omission of the shipped `[ModuleInitializer]` module-load path. Less load-bearing (it states the *requirement*, which the code still satisfies), but it no longer fully describes the implemented mechanism. | After ADR-0006 is reconciled, align this rule sentence to mention the module-load registration path. Infra/rules file — route through the proper channel, not this review. |

## Verdict
CONCERNS: 4

Reconciled and actual (no findings): stack.html test rows match the installed package set exactly (NUnit 4.6.1, NUnit3TestAdapter 6.2.0, FluentAssertions 7.2.2, Microsoft.NET.Test.Sdk 17.14.1, AltCover global tool; FA held on 7.x by license) — verified against `LordKuper.Common.Tests.csproj`. ADR-0007 and the in-place-remapped ADR-0001 accurately describe the StaticState seam as implemented (`StaticStateTestBase` ctor→`[SetUp]`/Dispose→`[TearDown]`, snapshot/restore set verbatim in `StaticStateFixture`, `[NonParallelizable]` on the three static-touching classes, no `[Collection]`/`[CollectionDefinition]`). The xUnit and coverlet tech-references are correctly marked SUPERSEDED with cross-links to ADR-0004/ADR-0007 and stack.html. `coverage.ps1` line 45 `--assemblyFilter nunit` (dead `coverlet` token retained per plan Task 6) is consistent with ADR-0004 and stack.html's Tooling row. `commands.yaml` test/coverage command strings are runner-agnostic and still resolve. PRD AC-19..AC-25/AC-27 trace to ADR-0006/ADR-0007 and to code.

## Next action
PM: route findings #1, #2, #4 to design-promote (domain creator: Architect) to reconcile ADR-0006, the NUnit tech-reference, and the coding rule with the as-built `[ModuleInitializer]`+`[OneTimeSetUp]` resolver, and to confirm the fallback adoption was approved (plan Task 2 required orchestrator sign-off; `state.json.escalations` is empty). Finding #3 (commands.yaml 80% floor) routes through the config/infra channel. None of these are code-side fixes; documentation reviewer does not write to persistent `design/` or infra files.

## Escalations
- finding #1: requires user/PM confirmation — the implemented resolver adopts the ADR-0006 "anticipated fallback" (`[ModuleInitializer]`) that the approved ADR records as not-chosen and gated on orchestrator approval; either the ADR is stale (design-promote fix) or an unrecorded scope/contract change occurred (escalation). PM to determine which.

REVIEW_DONE — CONCERNS: 4 (ADR-0006 + NUnit reference + coding rule describe a `[SetUpFixture]`/`[OneTimeSetUp]`-only resolver, but code ships the `[ModuleInitializer]` fallback live; commands.yaml still cites a stale >=80% coverage floor)
